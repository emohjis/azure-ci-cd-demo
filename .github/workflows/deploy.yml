name: Deploy to Azure VM

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Deploy to VM via SSH
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: 4.213.2.60
        username: azureuser
        password: Jishan@12345!
        script: |
          cd ~/myapp
          git pull origin main
          echo "Deployed successfully"
